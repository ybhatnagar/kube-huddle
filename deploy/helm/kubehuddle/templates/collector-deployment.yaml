{{- if .Values.collector.service.enabled }}
# Long-running collector trigger service (POST /ingest) for on-demand collection.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kubehuddle.fullname" . }}-collector-svc
  labels:
    {{- include "kubehuddle.labels" . | nindent 4 }}
    app.kubernetes.io/component: collector-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "kubehuddle.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: collector-svc
  template:
    metadata:
      labels:
        {{- include "kubehuddle.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: collector-svc
    spec:
      serviceAccountName: {{ include "kubehuddle.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
        runAsUser: 65532
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: collector
          image: "{{ .Values.images.collector.repository }}:{{ .Values.images.collector.tag }}"
          imagePullPolicy: {{ .Values.images.collector.pullPolicy }}
          args:
            - serve
            - --addr
            - ":{{ .Values.collector.service.port }}"
            - --prom-url
            - {{ .Values.collector.promUrl | quote }}
            - --interaction-source
            - {{ .Values.collector.interactionSource | quote }}
            - --cluster
            - {{ .Values.collector.cluster | quote }}
          env:
            {{- include "kubehuddle.dbEnv" . | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.collector.service.port }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          securityContext:
            {{- include "kubehuddle.containerSecurityContext" . | nindent 12 }}
          resources:
            {{- toYaml .Values.collector.serviceResources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubehuddle.fullname" . }}-collector-svc
  labels:
    {{- include "kubehuddle.labels" . | nindent 4 }}
    app.kubernetes.io/component: collector-svc
spec:
  selector:
    {{- include "kubehuddle.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: collector-svc
  ports:
    - name: http
      port: {{ .Values.collector.service.port }}
      targetPort: http
{{- end }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubehuddle.engine.fullname" . }}
  labels:
    {{- include "kubehuddle.labels" . | nindent 4 }}
    app.kubernetes.io/component: engine
spec:
  type: {{ .Values.engine.service.type }}
  selector:
    {{- include "kubehuddle.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: engine
  ports:
    - name: http
      port: {{ .Values.engine.service.port }}
      targetPort: http
